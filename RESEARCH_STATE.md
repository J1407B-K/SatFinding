# Research State

Only `results/current_era_v1/` is an active scientific source. Legacy experiments were retired because they are not reproducible under the current executable evidence contract. See `LEGACY_TOMBSTONE_INDEX.*`.

# Hook qualification
The scoped hook selects only an actually pending trail item, preserves assignment and reason semantics, and allows one reorder. Native and observe-only routes are equivalent; legal reorder smoke completed with VERIFIED proof.

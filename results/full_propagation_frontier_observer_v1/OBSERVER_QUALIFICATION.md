# Observer qualification

`FULL_FRONTIER_OBSERVER_QUALIFIED = true`. The isolated native observer is read-only, records pending as `trail[qhead:]`, uses canonical clause-literal reasons, and continues through conflict analysis. T8 and T10 OFF/ON routes are exact-equivalent with VERIFIED proofs; cross-run canonical frontier traces are stable.

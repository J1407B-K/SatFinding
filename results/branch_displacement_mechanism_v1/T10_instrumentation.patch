--- /private/tmp/satfinding-fixed-state-action-surface/glucose-3.0/core/Solver.cc
+++ /Users/kqs-mac/Go/GoProjects/SatFinding/results/branch_displacement_mechanism_v1/build_T10/glucose-3.0/core/Solver.cc
@@ -42,7 +42,7 @@
 #include "utils/System.h"
 
 using namespace Glucose;
-#include "/Users/kqs-mac/Go/GoProjects/SatFinding/fixed_state_action_surface.inc"
+#include "/Users/kqs-mac/Go/GoProjects/SatFinding/results/branch_displacement_mechanism_v1/build_T10/observer.inc"
 
 //=================================================================================================
 // Options:
@@ -779,6 +779,7 @@
     assigns[var(p)] = lbool(!sign(p));
     vardata[var(p)] = mkVarData(from, decisionLevel());
     trail.push_(p);
+    bm_event(from==CRef_Undef?"enqueue":"propagated_assignment",p,from,nullptr);
     hc_observe();
 }
 
@@ -803,6 +804,7 @@
     while (qhead < trail.size()){
         Lit            p   = trail[qhead++];     // 'p' is enqueued fact to propagate.
         cf_dequeue(p);
+        ++dequeues;bm_event("dequeue",p,CRef_Undef,nullptr);
         vec<Watcher>&  ws  = watches[p];
         Watcher        *i, *j, *end;
         num_props++;
@@ -1053,6 +1055,7 @@
     unsigned int nblevels,szWoutSelectors;
     bool blocked=false;
     starts++;
+    bm_event("restart",lit_Undef,CRef_Undef,nullptr);
     for (;;){
         hc_boundary();
         CRef confl = propagate();
@@ -1060,6 +1063,7 @@
             // CONFLICT
 	  conflicts++; conflictC++;conflictsRestarts++;
           cf_conflict(confl);
+          bm_event("conflict_clause",lit_Undef,confl,nullptr);
 	  if(conflicts%5000==0 && var_decay<0.95)
             var_decay += 0.01;
 
@@ -1086,12 +1090,16 @@
 	    selectors.clear();
             analyze(confl, learnt_clause, selectors,backtrack_level,nblevels,szWoutSelectors);
             cf_analysis(learnt_clause);
+            bm_event("conflict_analysis",lit_Undef,CRef_Undef,&learnt_clause);
 
 	    lbdQueue.push(nblevels);
 	    sumLBD += nblevels;
  
 
             cancelUntil(backtrack_level);
+            bm_event("backtrack",lit_Undef,CRef_Undef,nullptr);
+            bm_event("learned_clause",lit_Undef,CRef_Undef,&learnt_clause);
+            bm_event("heap_activity",lit_Undef,CRef_Undef,nullptr);
 
             if (certifiedUNSAT) {
               for (int i = 0; i < learnt_clause.size(); i++)
@@ -1166,7 +1174,7 @@
             if (next == lit_Undef){
                 // New variable decision:
                 decisions++;
-                next = pickBranchLit();
+                next = bm_pick();
 
                 if (next == lit_Undef){
 		  printf("c last restart ## conflicts  :  %d %d \n",conflictC,decisionLevel());
@@ -1176,6 +1184,7 @@
             }
 
             cf_decision(next);
+            bm_event("decision",next,CRef_Undef,nullptr);
             // Increase decision level and enqueue 'next'
             newDecisionLevel();
             uncheckedEnqueue(next);
--- /private/tmp/satfinding-fixed-state-action-surface/glucose-3.0/core/Solver.h
+++ /Users/kqs-mac/Go/GoProjects/SatFinding/results/branch_displacement_mechanism_v1/build_T10/glucose-3.0/core/Solver.h
@@ -283,6 +283,9 @@
     void hc_gc();
     unsigned long long hc_state_hash();
     void hc_before_enqueue();
+    void bm_start();
+    void bm_event(const char*,Lit,CRef,const vec<Lit>*);
+    Lit bm_pick();
     void hc_boundary();
     void fs_surface();
     void cf_dequeue(Lit);
--- /private/tmp/satfinding-fixed-state-action-surface/glucose-3.0/mtl/Heap.h
+++ /Users/kqs-mac/Go/GoProjects/SatFinding/results/branch_displacement_mechanism_v1/build_T10/glucose-3.0/mtl/Heap.h
@@ -1,3 +1,4 @@
+#include <vector>
 #include <cstdio>
 /******************************************************************************************[Heap.h]
 Copyright (c) 2003-2006, Niklas Een, Niklas Sorensson
@@ -77,6 +78,7 @@
 
     unsigned long long lt_hash_state() const {unsigned long long h=1469598103934665603ULL;auto add=[&](int x){for(unsigned k=0;k<sizeof(x);++k)h=(h^((unsigned char*)&x)[k])*1099511628211ULL;};add(heap.size());for(int i=0;i<heap.size();++i)add(heap[i]);add(indices.size());for(int i=0;i<indices.size();++i)add(indices[i]);return h;}
     void lt_dump(FILE* f) const {fprintf(f,"{\"array\":[");for(int i=0;i<heap.size();++i)fprintf(f,"%s%d",i?",":"",heap[i]);fprintf(f,"],\"indices\":[");for(int i=0;i<indices.size();++i)fprintf(f,"%s%d",i?",":"",indices[i]);fprintf(f,"]}");}
+    std::vector<int> semantic_order() const { Heap copy(lt); heap.copyTo(copy.heap); indices.copyTo(copy.indices); std::vector<int> out; while(!copy.empty()) out.push_back(copy.removeMin()+1); return out; }
     Heap(const Comp& c) : lt(c) { }
 
 
--- /Users/kqs-mac/Go/GoProjects/SatFinding/fixed_state_action_surface.inc
+++ /Users/kqs-mac/Go/GoProjects/SatFinding/results/branch_displacement_mechanism_v1/build_T10/observer.inc
@@ -26,6 +26,71 @@
 void hc_init(const char* path,int which){cf_dir=path;cf_full_at=which;hc_out=fopen((cf_dir+"/opportunities.jsonl").c_str(),"w");if(!hc_out)throw std::runtime_error("LOG_OPEN");}
 static void cf_stop(const char* status){fprintf(hc_out,"{\"event\":\"STOP\",\"status\":\"%s\",\"opportunity\":%d}\n",status,cf_index);fflush(hc_out);_exit(0);}
 void hc_finish(){if(cf_child&&!cf_full){fprintf(hc_out,"{\"event\":\"NO_NEXT_CONFLICT\"}\n");fflush(hc_out);_exit(44);}fprintf(hc_out,"{\"event\":\"FINISH\",\"interventions\":%d}\n",cf_child?cf_role:0);fclose(hc_out);}
+
+#include <iomanip>
+#include <sys/stat.h>
+static HU dequeues=0;
+static std::string output_dir;
+static int lit(Lit p){return hc_lit(p);}
+template<class T> static void array(std::ostream&o,const T&v){o<<'[';bool first=true;for(auto x:v){if(!first)o<<',';first=false;o<<x;}o<<']';}
+// Read-only event observer, plus two explicit legal decision interventions.
+static HU bm_index=0,bm_d0=0,bm_c0=0,bm_q0=0,bm_decision_ordinal=0;
+static bool bm_active=false,bm_injection=false,bm_changed=false;
+static int bm_var=0,bm_target_ordinal=0,bm_target_lit=0;
+static std::string bm_mode;
+static std::ofstream bm_log;
+void Solver::bm_event(const char* type, Lit p, CRef cr, const vec<Lit>* clause){
+ if(!bm_active || bm_index>=10000)return;
+ std::vector<int> cs;
+ if(clause){for(int j=0;j<clause->size();j++)cs.push_back(lit((*clause)[j]));}
+ else if(cr!=CRef_Undef){const Clause& c=ca[cr];for(int j=0;j<c.size();j++)cs.push_back(lit(c[j]));}
+ std::sort(cs.begin(),cs.end());
+ int v=bm_var-1,greater=0,equal=0,rank=0,top=0;auto heap=order_heap.semantic_order();
+ for(int j=0;j<nVars();j++){greater+=activity[j]>activity[v];equal+=activity[j]==activity[v];}
+ for(size_t j=0;j<heap.size();j++){int u=heap[j]-1;if(u==v)rank=j+1;if(!top&&value(u)==l_Undef&&decision[u])top=u+1;}
+ bm_log<<"{\"index\":"<<bm_index++<<",\"type\":\""<<type<<"\",\"literal\":"<<(p==lit_Undef?0:lit(p))<<",\"clause\":";array(bm_log,cs);
+ bm_log<<",\"injection\":"<<(bm_injection?"true":"false")<<",\"decisions\":"<<decisions-bm_d0<<",\"decision_ordinal\":"<<bm_decision_ordinal<<",\"dequeues\":"<<dequeues-bm_q0<<",\"conflicts\":"<<conflicts-bm_c0<<",\"level\":"<<decisionLevel()<<",\"activity\":"<<std::setprecision(17)<<activity[v]<<",\"activity_rank\":"<<(greater+(equal+1)/2.0)<<",\"heap_rank\":"<<rank<<",\"heap_member\":"<<(order_heap.inHeap(v)?"true":"false")<<",\"top_candidate\":"<<top<<",\"value\":"<<toInt(value(v));
+ if(std::string(type)=="heap_activity"||std::string(type)=="checkpoint"){
+  bm_log<<",\"heap_order\":";array(bm_log,heap);bm_log<<",\"activity_vector\":[";
+  for(int j=0;j<nVars();j++)bm_log<<(j?",":"")<<activity[j];bm_log<<']';
+ }
+ bm_log<<"}\n";
+}
+void Solver::bm_start(){
+ bm_var=atoi(getenv("BM_VAR"));bm_mode=getenv("BM_MODE");bm_target_ordinal=atoi(getenv("BM_ORDINAL"));bm_target_lit=atoi(getenv("BM_LITERAL"));
+ if(bm_var<1||bm_var>nVars())throw std::runtime_error("MECHANISM_REQUEST");
+ bm_log.open(output_dir+"/events.jsonl");bm_active=true;bm_d0=decisions;bm_c0=conflicts;bm_q0=dequeues;
+ bm_event("checkpoint",lit_Undef,CRef_Undef,nullptr);
+}
+Lit Solver::bm_pick(){
+ if(!bm_active)return pickBranchLit();
+ ++bm_decision_ordinal;
+ if(!bm_changed && (int)bm_decision_ordinal==bm_target_ordinal && bm_mode=="restore"){
+  Lit p=mkLit(abs(bm_target_lit)-1,bm_target_lit<0);
+  if(value(p)==l_Undef&&decision[var(p)]){
+   if(random_var_freq!=0||rnd_pol)throw std::runtime_error("CF_REQUIRES_DETERMINISTIC_BRANCHING");
+   // Native pickBranchLit consumes exactly one RNG draw even at random_var_freq=0.
+   (void)drand(random_seed);
+   bm_changed=true;bm_event("restoration",p,CRef_Undef,nullptr);
+   // Retain ordinary lazy removal semantics by removing this selected variable.
+   // Native forced choice may leave it in the heap; assigned entries are skipped normally.
+   return p;
+  }
+  bm_event("restoration_unavailable",p,CRef_Undef,nullptr);
+ }
+ Lit p=pickBranchLit();
+ if(!bm_changed && (int)bm_decision_ordinal==bm_target_ordinal && bm_mode=="suppress" && p!=lit_Undef && var(p)==abs(bm_target_lit)-1){
+  if(random_var_freq!=0||rnd_pol)throw std::runtime_error("CF_REQUIRES_DETERMINISTIC_BRANCHING");
+  // Continue the same native heap scan without a second random draw.
+  Var av=var_Undef;
+  while(!order_heap.empty()){Var u=order_heap.removeMin();if(value(u)==l_Undef&&decision[u]){av=u;break;}}
+  Lit alternative=av==var_Undef?lit_Undef:mkLit(av,polarity[av]);
+  if(alternative!=lit_Undef){insertVarOrder(var(p));bm_changed=true;bm_event("suppression",p,CRef_Undef,nullptr);return alternative;}
+  bm_event("suppression_unavailable",p,CRef_Undef,nullptr);
+ }
+ return p;
+}
+
 void Solver::hc_register(CRef cr){hc_ids[cr]=++hc_ids_next;}
 void Solver::hc_gc(){std::map<CRef,HU> next;for(const auto& e:hc_ids){if(!ca[e.first].reloced())throw std::runtime_error("ID_RELOCATION");next[ca[e.first].relocation()]=e.second;}hc_ids.swap(next);}
 HU Solver::hc_state_hash(){
@@ -81,6 +146,7 @@
  // All selection records are durable before any branch starts.
  fflush(NULL);
  for(int i=-1;i<take;++i){
+  if(i>=0&&i+1!=atoi(getenv("BM_ACTION")))continue;
   if(hc_state_hash()!=state || hc_hash(this,sizeof(Solver))!=raw)throw std::runtime_error("PARENT_STATE_MUTATED");
   fs_tag=i<0?"BASELINE":"ACTION_"+std::to_string(i+1);
   fflush(NULL);pid_t pid=fork();if(pid<0)throw std::runtime_error("FORK_FAILED");
@@ -92,8 +158,10 @@
    long pos=ftell(certifiedOutput);if(pos<0)_exit(47);FILE* dst=fopen(cf_path(0,0,".drup").c_str(),"w");if(!dst)_exit(48);
    std::ifstream src(cf_dir+"/prefix.drup",std::ios::binary);for(long k=0;k<pos;++k){int ch=src.get();if(ch==EOF)_exit(49);fputc(ch,dst);}certifiedOutput=dst;
    if(!freopen(cf_path(0,0,".stdout.txt").c_str(),"w",stdout))_exit(50);
+   output_dir=cf_dir+"/"+fs_tag;mkdir(output_dir.c_str(),0755);bm_start();
+   if((i<0&&bm_mode=="restore")||(i>=0&&bm_mode=="suppress"))bm_mode="observe";
    if(i>=0){const auto& a=actions[i];const Clause& c=ca[a.cr];if(value(a.p)!=l_Undef || !(c.size()==2||(c.size()>2&&c[0]==a.p)))_exit(51);for(int j=0;j<c.size();++j)if(c[j]!=a.p&&value(c[j])!=l_False)_exit(52);
-    uncheckedEnqueue(a.p,a.cr);fprintf(hc_out,"{\"event\":\"EARLY_ENQUEUE\",\"literal\":%d,\"reason_id\":%llu,\"global_enqueue\":%llu}\n",hc_lit(a.p),a.id,hc_enqueues);fflush(hc_out);
+    bm_injection=true;uncheckedEnqueue(a.p,a.cr);bm_injection=false;fprintf(hc_out,"{\"event\":\"EARLY_ENQUEUE\",\"literal\":%d,\"reason_id\":%llu,\"global_enqueue\":%llu}\n",hc_lit(a.p),a.id,hc_enqueues);fflush(hc_out);
    }
    return;
   }

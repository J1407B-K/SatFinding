# Trajectory divergence timing analysis

Existing T6–T14 CONTROL/TREATMENT event ledgers were read without rerunning or intervening. The ledgers do not expose independently auditable first reason, analysis, activity, heap, propagation, learned-clause, or decision divergence fields. Those classes are marked `UNAVAILABLE`; no instrumentation was added.

All nine targets are preserved. The requested timestamps are unavailable, so this cohort cannot support timing correlations or claims about which layer separates fast from slow cases. First treatment reason use cannot be assessed as a predictor. No causal ordering is inferred.

A. Earliest universal divergence layer: undetermined.
B. Layer most associated with final ops gap: undetermined.
C. Layers unable to distinguish fast/slow: cannot assess from these fields.
D. The reason-path-to-heuristic-state hypothesis remains worth investigating, but this analysis cannot choose between reason, learned clauses, and heuristic state.

# Decision828 context, frozen observation protocol
Only the four existing valid configurations A, B, A_FORCED(-570), B_FORCED(-187).
Reuse exact valid branch-entry override including one native RNG draw and lazy
heap behavior. No new target, set, selector, parameters or injection sweep.
Bound event detail to completed/current conflict600..800. Checkpoints at complete
conflict boundaries600,650,656,657,700,750,800 and decision828 pre-pick; response
boundaries658,662,667,677,707 correspond to first1/5/10/20/50 conflicts after828.
700/750/800 occur AFTER decision828 (completed conflict657), not before it.
Record enqueue/reason, conflict, UIP/minimization, activity/heap hashes and bumped
value, learned content/LBD, backjump/restart, decisions; full logical snapshots.
All four full native counters and proof hashes must reproduce frozen valid runs.
Natural trace before any new intervention. At most one additional candidate-state
intervention, only if a specific trace-linked ingredient is identified, legality
can be established, and precise mutation is frozen first. Otherwise do none.

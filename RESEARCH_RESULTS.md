# 第一轮研究结论

日期：2026-09-09。本文件区分已经证明的性质、实际测量和仍未覆盖的范围。

## 1. Soundness

[完整证明](docs/soundness.md)给出了 SAT 模型检查、resolution 归纳证明、任意候选检索器的
局部 soundness，以及 residual 假设作用域的组合论证。检索、映射或历史内容错误不能绕过
最终检查器接受错误结论。该结论以输入语义、检查器及其执行正确为前提，不是硬件或任意
Python 代码执行攻击下的无条件保证；尚无机器验证的实现精化证明。

16 项测试通过，其中包括全部 512 个双变量非重言子句集合的求解，以及 512×512 =
262,144 次跨公式证书检查。还覆盖伪造证明、错误前提、恶意缓存返回、分支 UNSAT
误用、变量重命名/极性变换，以及生成器输入的规范化。

## 2. 真实基准上的净加速

使用官方 SATLIB 的全部 500 个 RTI 和全部 500 个 BMS 文件，未改写题目。
先处理所有 RTI，再以固定随机顺序处理 BMS。BMS 是官方提供的对应 RTI 子公式，
因此这是有意选择的关联工作负载，而非独立来源泛化实验。
检索不接收配对 ID 或文件名，使用变量编号对齐的稀疏子句向量。

每种策略完整执行 5 轮，轮换策略运行顺序。下表是整轮耗时中位数；包含求解、建库、
检索、筛除、完整证书检查和缓存插入。所有策略共同的文件加载/规范化及结果文件输出
不在计时窗口内。基础求解器为 Glucose3。

| 策略 | 完整 1000 个实例耗时 | BMS 命中数 |
|---|---:|---:|
| 无缓存，同样检查证书 | 2.905 s | 0 |
| Exact | 3.289 s | 0 |
| Canonical，精确有色图规范化 | 4.969 s | 0 |
| Semantic，子句向量 + 快速拒绝 + 完整检查 | 1.955 s | 500 |
| 结构宽度/度数直方图 | 7.074 s | 0 |

相对同样验证证书的无缓存方案：**1.486×，总时间下降约 32.7%**。
五轮逐轮速度比为 1.474、1.480、1.410、1.434、1.486；不把这些五次测量当作总体置信区间。
共同加载耗时为 0.640 s；把它分别计入双方，总速度比约 1.366×。

更强的额外参考：单独测量不计验证时间的裸 Glucose（初始化、求解、取模型、销毁），
5 轮中位数为 2.100 s。完整缓存流程相对它为 1.074×，总时间下降约 6.9%。
这个较小收益来自另一次顺序测量，不应解读成跨机器稳定的性能保证。

负结果也保留：优化前虽然命中 500/500，总耗时却为 3.145 s，慢于当轮无缓存的
2.898 s。优化只增加了候选模型的快速拒绝，仍然对每个接受的证书执行完整检查。
冷启动 RTI 阶段没有任何命中，优化后的 semantic 此阶段为 1.446 s，慢于无缓存
的 0.932 s。因此没有证据说明该缓存对无关联实例有益。

原始记录：[优化前](results/satlib.json)、[优化后](results/satlib-optimized.json)、
[裸求解器参考](results/native-reference.json)。包含来源 URL、文件哈希、分实例时间和
软件版本；优化后记录还含运行代码哈希。

## 3. 超出 exact/canonical 的合法复用

500 次命中全部具有不同的源/目标子句数，因此不可能是变量重命名、独立极性翻转或
子句排列所产生的同构；这比“canonical 实现没有找到命中”更强，是可直接检查的反证。
例如 RTI_15 有 429 个子句，而 BMS_15 有 268 个子句，历史模型满足当前全部子句。

[独立复验产物](results/reuse-certificates.json)保存了 500 个重新从历史源求出的赋值及
源/目标 SHA-256；每个赋值均再次验证目标。它们不是计时执行时模型的逐字日志，而是
对同一组额外合法复用关系的独立重建。[审计脚本](audit_benchmark.py)可重复该过程。

这里证明的是模型证书可复用的范围超过整公式同构。稀疏子句向量依赖稳定变量编号，
尚未证明神经 embedding 有收益，也未证明预算内 heuristic 的命中集合包含所有 canonical
命中。通用 proof motif、任意重命名匹配和真实 UNSAT 基准迁移尚未完成。

## 4. 结构类上的复杂度下降

[理论证明](docs/complexity.md)构造了“固定不可满足 core + n 个独立可满足填充子句”的
公式族。对规定变量顺序、仅做单元传播且不学习/分解/消纯文字的 DPLL，决策节点数严格为
2^(n+1)-1；复用常数大小 core 证明后，规范化输入的查找与验证为期望 O(n)，包含排序
或使用确定性树索引则为 O(n log n)，忽略通常的整数位成本因子。

[有限执行记录](results/theory.json)验证了 n=0,2,...,14 的递推；n=14 为 32,767
个决策节点。它不是对现代 CDCL 或所有 SAT 算法的指数下界：组件分解、消纯文字等
本身就能处理这类填充结构。证明还给出了重复 core 的摊销成本 S+Σ(M_i+V_i)，明确把
历史求证、匹配和检查成本都计入。

## 当前可支持的主张

已经得到条件明确的 soundness 证明、官方关联 SAT 数据上的净加速、500 个超出同构的
合法复用证据，以及一个相对明确 DPLL 算法的复杂度分离定理。还不能主张通用 SAT
加速、学习式结构检索优越性、生产级 UNSAT proof-transfer 收益，或 P=NP。

来源：[SATLIB 数据说明](https://www.cs.ubc.ca/~hoos/SATLIB/Benchmarks/SAT/BMS/descr_BMS.html)、
[PySAT 接口](https://pysathq.github.io/docs/api/solvers.html)、
[pynauty](https://github.com/pdobsan/pynauty)。
